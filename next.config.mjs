/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.nexusberry.com",
      "images.unsplash.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
